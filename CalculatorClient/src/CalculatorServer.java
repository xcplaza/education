import telran.server.ProtocolJava;
import telran.server.ServerJava;

public class CalculatorServer extends ServerJava {

	public CalculatorServer(ProtocolJava protocol, int port) throws Exception {
		super(protocol, port);
	}
}
